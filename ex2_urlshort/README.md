# Exercise 2 - Url redirect

NOTE: I'm using json instead of YAML to read url mappings from file.

Also, I've used gorilla mux for this project.

## Usage
```
go run main.go -urls ./url-mapping.json
```
